package org.dna.draw.dialog

import android.content.Context

class DialogColorSize(foContext: Context, foListener: OnSelectBrushColorListener) {

    private val poContext: Context = foContext
    private val poListener: OnSelectBrushColorListener = foListener

    init {
        
    }
}