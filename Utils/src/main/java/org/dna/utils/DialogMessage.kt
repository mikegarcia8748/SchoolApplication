package org.dna.utils

import android.content.Context

class DialogMessage (context: Context){

    private val poContext = context
    private var mListener: OnMessageDialogActionClickListener? = null

    init {

    }
}